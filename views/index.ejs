<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Legacy Lines</title>
  </head>
  <body>
    <section>
      <% authorsArray.forEach(author => { %> <%= author.name %> <br />
      <% }) %>
    </section>
    <section>
      <% firstAuthorQuotesArray.forEach(quote => { %> <%= quote.quote %> <br />
      <% }) %>
    </section>
  </body>
</html>
